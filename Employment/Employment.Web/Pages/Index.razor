@page "/Employment"
@inherits BasePage
@{
    base.BuildRenderTree(__builder);
}
@using Employment.Web.Pages
<ComboBox TValue="int" TEntity="EmploymentOrderType" TextExpression="t => t.Title" OnValueChanged="async () => await ChangeValue()" />

@code{
    EmploymentOrder order;

    async Task ChangeValue()
    {
        if (await Confirm("آیا با ثبت موافقید؟"))
        {
            
        }
    }
}

