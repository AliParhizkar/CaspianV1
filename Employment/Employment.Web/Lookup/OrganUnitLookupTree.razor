@namespace Employment.Web
@inherits LookupTreePage<OrganUnit>
<CTreeView @ref="Tree" SelectedNodesValue="SelectedNodesValue" TEntity="OrganUnit" TextFunc="t => t.Title" Selectable
    ParentNodeFilterFunc="t => t.ParentOrganId == null" 
    FilterFunc="t => (!SearchString.HasValue() || t.Title.StartsWith(SearchString))"
    style="margin:3px auto;width:350px;;height:calc(100vh - 50px)" />
@code 
{
    
}
