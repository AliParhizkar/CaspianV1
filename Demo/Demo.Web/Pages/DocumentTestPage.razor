@page "/Demo/DocumentTest/{FileName}/{FolderName}"
@attribute [Source(89, "Document Test")]
@inherits BasePage

<Document Page="this" FileName="@FileName" FolderName="@FolderName" /> 
@code 
{
    [Parameter]
    public string FolderName { get; set; }

    [Parameter]
    public string FileName { get; set; }
}
