@namespace Caspian.Engine.Account

@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo($"Account/Login?returnUrl1={Uri.EscapeDataString(NavigationManager.Uri)}", forceLoad: true);
    }
}
