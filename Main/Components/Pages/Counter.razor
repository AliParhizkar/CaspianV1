@page "/counter"
@using Caspian.Engine.Service
@using Demo.Service
@inherits BasePage



@code 
{
    CustomerType customerType;

    protected override async Task OnInitializedAsync()
    {
        using var scope = CreateScope();
        var query = scope.GetService<OrderDetailService>().GetAll().Where(t => t.Order.CustomerId != null);
        var list = await new ReportEngine(scope).GetData(2, query);
        await base.OnInitializedAsync();
    }
}
