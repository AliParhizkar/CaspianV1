@inherits BasePage
@using Demo.Model
@using Demo.Service
@using Microsoft.EntityFrameworkCore
@page "/counter"
<button>Test</button>
@code
{
    protected override async Task OnInitializedAsync()
    {
        using var scope = CreateScope();
        var service = scope.GetService<WareHouseReceiptService>();
        var recipt = new WarehouseReceipt() {Id = 13171 };
        var data = service.GetDetailsAfterAddChangesAsync(recipt);
        await base.OnInitializedAsync();
    }
}
