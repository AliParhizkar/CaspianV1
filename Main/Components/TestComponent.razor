@page "/Test1"
@using Caspian.UI
@using Demo.Model
@using Demo.Service
@using Engine.Model
@using Microsoft.EntityFrameworkCore
@using System.Linq.Expressions
@inherits BasePage


@code 
{


    protected override async Task OnInitializedAsync()
    {
        using var context = new Context();
        var query = context.Orders.Join(context.PersianDatesConvertor, date => date.Date, pdate => pdate.Date, (date, pdate) => new
        {
            Date = date, PDate = pdate
        });
        var str = query.ToQueryString();
        await base.OnInitializedAsync();
    }

    IQueryable<TResult> Join123<TOuter, TInner, TKey, TResult>(Expression<Func<TInner, TKey>> innerKeySelector, Expression<Func<TOuter, TInner, TResult>> resultSelector)
    {
        return null;
    }
}
