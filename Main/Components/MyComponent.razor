@namespace Main.Test
<button @onclick="() => Counter++">Add</button>
<span>@Counter</span>
@code 
{
    async Task Changed()
    {
        if (CounterChanged.HasDelegate)
            await CounterChanged.InvokeAsync(Counter + 1);
    }

    [Parameter]
    public int Counter { get; set; }

    [Parameter]
    public EventCallback<int> CounterChanged { get; set; }
}
