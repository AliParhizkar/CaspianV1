@namespace Caspian.UI
@typeparam TEntity where TEntity : class
@inject IServiceScopeFactory ScopeFactory

<Window @bind-Status="status" @attributes="windowProperties" >
    <CaspianForm @ref="form" TEntity="TEntity" Model="entity" ColumnsCount="ColumnsCount" OnSubmit="UpdateEntity">
        <CaspianValidationValidator TModel="TEntity" ValidatorType="serviceType" />
        @ChildContent(newEntity)
   </CaspianForm>
</Window>