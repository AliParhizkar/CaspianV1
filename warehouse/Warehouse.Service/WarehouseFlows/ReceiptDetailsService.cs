
using Caspian.Common;
using Caspian.Common.Service;
using Warehouse.Model;

namespace Warehouse.Service
{
    //public class ReceiptDetailsService : BaseService<ReceiptDetails>
    //{
    //    public ReceiptDetailsService(IServiceProvider provider)
    //        :base(provider)
    //    {
    //        RuleFor(t => t.Quantity).Custom(t => t.Quantity <= 0, "تعداد/مقدار باید بزرگتر از صفر باشد");
    //        RuleFor(t => t.ExpireDate).Custom(t => t.ExpireDate < DateTime.Now.GetDateOnly(), "تاریخ انقضا باید بزرگتر مساوی تاریخ روز باشد");
    //    }
    //}
}
